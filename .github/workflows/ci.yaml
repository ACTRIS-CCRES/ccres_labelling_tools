name: CI

on:
  push:
    paths-ignore:
      - "docs/**"
      - "*.md"
      - "*.rst"
      - ".readthedocs.yaml"

  pull_request:
    paths-ignore:
      - "docs/**"
      - "*.md"
      - "*.rst"
      - ".readthedocs.yaml"

jobs:
  pre-commit:
    name: Run pre-commit
    # This is the VM in which github run
    # We can use a custom container that will
    # be run in this VM
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repository
        uses: actions/checkout@v7
        with:
          lfs: true

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.14"

      - name: Cache python environment
        uses: actions/cache@v5
        # https://tobiasmcnulty.com/posts/caching-pre-commit/
        # This restore and/or save in the same action
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-python-3.14-venv-${{ hashFiles('pyproject.toml') }}

      - name: Install dependencies
        run: |
          python -m pip install -U . --group dev

      - name: run pre-commits
        uses: pre-commit/action@v3.0.1

  multi-os-tests:
    if: ( github.ref == format('refs/heads/{0}', github.event.repository.default_branch) ) || startsWith(github.ref, 'refs/tags')
    name: ${{ matrix.session }} ${{ matrix.python }} / ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    needs: pre-commit
    strategy:
      fail-fast: false
      matrix:
        include:
          - {
              python: "3.12",
              os: "ubuntu-latest",
              session: "tests",
              cache-path: ~/.cache/pip,
            }
          - {
              python: "3.13",
              os: "ubuntu-latest",
              session: "tests",
              cache-path: ~/.cache/pip,
            }

    env:
      FORCE_COLOR: "1"
      PRE_COMMIT_COLOR: "always"

    steps:
      - name: Check out the repository
        uses: actions/checkout@v7
        with:
          lfs: true

      - name: Set up Python ${{ matrix.python }}
        uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python }}

      - name: Upgrade pip
        run: |
          python -m pip install pip
          python -m pip --version

      - name: Cache python environment
        uses: actions/cache@v5
        with:
          path: |
            tests/data/outputs/
            ${{ matrix.cache-path }}
          key: ${{ runner.os }}-python-${{ matrix.python }}-pip-${{ hashFiles('pyproject.toml') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install -U . --group dev

      - name: Run tests
        run: |
          python -m pytest

  linux-tests:
    name: Tests for Python 3.14 on ubuntu-latest
    runs-on: ubuntu-latest
    needs: pre-commit
    # If not default branch
    # It allows us to not use too much walltime of github
    env:
      FORCE_COLOR: "1"
      PRE_COMMIT_COLOR: "always"

    steps:
      - name: Check out the repository
        uses: actions/checkout@v7
        with:
          lfs: true

      - name: Set up Python 3.14
        uses: actions/setup-python@v7
        with:
          python-version: "3.14"

      - name: Cache python environment
        uses: actions/cache@v5
        # https://tobiasmcnulty.com/posts/caching-pre-commit/
        # This restore and/or save in the same action
        with:
          path: |
            tests/data/outputs/
            ~/.cache/pip
          key: ${{ runner.os }}-python-3.14-pip-${{ hashFiles('pyproject.toml') }}

      - name: Install dependencies
        run: |
          python -m pip install -U . --group dev

      - name: Run tests
        run: |
          python -m pytest

      - name: Upload artifacts generated by tests
        uses: actions/upload-artifact@v7
        with:
          name: tests-artifacts
          path: tests/data/outputs/

      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: false
          files: ./coverage.xml
          verbose: true
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

  upload-tests-artifacts:
    name: Upload artifacts generated by tests
    runs-on: ubuntu-latest
    needs: linux-tests
    steps:
      - name: Check out the repository
        uses: actions/checkout@v7

      - uses: actions/upload-artifact@v7
        with:
          name: tests-artifacts
          path: tests/data/outputs/

  test-pypi:
    name: Deploy Package to Test PyPI
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    needs: [pre-commit]
    environment:
      name: testpypi
      url: https://testpypi.org/p/ccres-labelling-tools/
    permissions:
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          lfs: true
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.14"

      - name: Install pypa/build
        run: |
          python -m pip install --upgrade build --user

      - name: Build a binary wheel and a source tarball
        run: |
          python -m build --sdist --wheel --outdir dist/ .

      - name: Publish package distributions to TestPyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/

  pypi:
    name: Deploy Package to PyPI
    # Deploy only tagged commit and if tests have succeeded
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags')
    runs-on: ubuntu-latest
    needs: linux-tests
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          lfs: true
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.14"

      - name: Install pypa/build
        run: |
          python -m pip install --upgrade build --user

      - name: Build a binary wheel and a source tarball
        run: |
          python -m build --sdist --wheel --outdir dist/ .

      - name: Publish package to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          password: ${{ secrets.PYPI_TOKEN }}
